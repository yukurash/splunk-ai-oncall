{
  "$comment": "Claude Code 用 Splunk Observability MCP。公式の mcp-remote ブリッジ経由。値は ${VAR} で環境変数から展開されるので、claude 起動前に .env をエクスポートすること（例: set -a; . ./.env; set +a; claude）。生トークンはここへ書かない。",
  "mcpServers": {
    "splunk-o11y": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-remote",
        "${SPLUNK_MCP_URL}",
        "--header",
        "X-SF-TOKEN: ${SPLUNK_ACCESS_TOKEN}",
        "--header",
        "X-SF-REALM: ${SPLUNK_REALM}"
      ]
    }
  }
}
