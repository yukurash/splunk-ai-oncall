# シナリオ4: adManualGc（間欠レイテンシ / GC）

- flag: `adManualGc` variant=on（ad サービスで強制フル GC を周期発生）
- 収集窓 -2m / env unknown / Ground Truth: **ad サービスの GC ストップ・ザ・ワールドによる間欠レイテンシ（エラーではない）**
- 証拠: `evidence.json` / `evidence-compact.md`。**ad のみ p50=689ms / p90=957ms / p99=1440ms**（他サービスは p50 一桁ms、エラー0）

## 採点
| 観点 | A: Claude+MCP | B: 素のClaude |
|---|:--:|:--:|
| 根本原因サービス | ✅ ad | ✅ ad |
| 障害の性質 | ✅ ad 自体のレイテンシ（p50突出）。エラーでなく遅延と明言 | ✅ ad の周期的フル GC ポーズ |
| 根拠 | **実データ**（p50突出＋adを含むスロートレース、load-gen/proxyノイズを除外） | 症状推論（全体・間欠＝共有経路の周期ポーズ）＋デモ知識 |
| 確信度 | 中（内部スパン未取得のため） | 中（実データで対立仮説を排除できず） |
| スコア(0-2) | **2** | **2** |

## 所見
- A は「**p50（中央値）が高い＝そのサービス自体が遅い**」という指標の読みで ad を断定し、load-generator（合成ブラウザ）や frontend-proxy（エッジの遅いPOST）のテールノイズを正しく除外。データに忠実。
- B は症状（全体・間欠）から「共有経路上の周期ポーズ」を推論し ad-GC に到達（＋デモ知識）。良い推論だが、ad を名指せたのはデモ構成の知識による面が大きい。
- A は "GC" までは断定せず（内部データ無し）正直に留保。B は GC と踏み込んだ（が確証なし）。
