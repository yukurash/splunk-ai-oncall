# アラート（ページ）テンプレート — A/B 共通

A（MCP あり）と B（MCP なし）に **完全に同じ文面**で渡す。サービス名・原因の示唆は書かない。

---

```
[PAGE] Astronomy Shop / 本番

発生時刻: <YYYY-MM-DD HH:MM JST>
重大度: SEV-2

症状:
<scenarios.md の「症状（ページに書く範囲）」をそのまま貼る>

依頼:
オンコールとして、この事象の "根本原因" を特定し、推奨対処までを報告してください。
```

---

## B（素の Claude）に追加で添える一文

> あなたには監視ツールへのアクセスはありません。このアラート文だけを手がかりに、
> Astronomy Shop（OpenTelemetry Demo の一般的な構成）の知識から、最も可能性の高い
> 根本原因を `oncall-system.md` と同じ出力フォーマットで答えてください。

## 運用メモ

- A・B は**別セッション**で実行し、互いの回答を見せない。
- 同じシナリオでも A→B の順で先入観が入らないよう、できれば B を先に取る。
- 開始/終了時刻を控える（採点の所要時間用）。Claude Code 側のトークン/コストも記録。
